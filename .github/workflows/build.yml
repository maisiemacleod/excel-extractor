name: Build Windows EXE
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install openpyxl xlrd tkinterdnd2 pyinstaller
      - name: Build EXE
        run: pyinstaller --onefile --noconsole --name excel_extractor excel_extractor.py
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: excel_extractor_exe
          path: dist/excel_extractor.exe
